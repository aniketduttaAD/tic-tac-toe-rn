import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
    container: {
        alignItems: "center",
        paddingTop: 5
    },
    logo: {
        alignSelf: "center",
    height: 350, 
    maxWidth: "85%",
        resizeMode: "contain"
    },
    buttons: {
        marginTop:50,
    },
    button: {
        marginBottom:10,
    }
});

export default styles;