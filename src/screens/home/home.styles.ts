import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
    container: {
        alignItems: "center",
        // paddingTop: 20
    },
    logo: {
        alignSelf: "center",
    height: 400, 
    maxWidth: "80%",
        resizeMode: "contain"
    },
    buttons: {
        marginTop:80,
    },
    button: {
        marginBottom:20,
    }
});

export default styles;